# Environment
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Tooling caches
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# Editors and OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# The live binding table carries real chat and user ids. The committed artifact
# is config/telegram_bindings.example.toml. In a real deployment this file
# belongs in version control too — in a private ops repository, so that changing
# who may act stays a reviewable diff.
config/telegram_bindings.toml

# Graph working directory. The committed map is .graphify/context.md.
graphify-out/

# Local scratch
scratch/
*.log
